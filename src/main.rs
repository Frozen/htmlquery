//extern crate htmlquery;
//extern crate html5ever;
//
//use html5ever::parse_document;
//use std::io;
////use htmlquery::{walk, Node};
//use html5ever::rcdom::{NodeData, RcDom, Handle};
//use html5ever::tendril::TendrilSink;
//use std::rc::Rc;
//use std::io::Cursor;
//use std::collections::HashMap;

fn main() {

    //    let mut x = "".to_string();
    //
    //    let stdin = io::stdin();
    //    let dom = parse_document(RcDom::default(), Default::default())
    //        .from_utf8()
    ////        .read_from(&mut Cursor::new(x.as_bytes()))
    //        .read_from(&mut stdin.lock())
    //        .unwrap();
    //
    //    let initial = Rc::new(Node::default());
    //
    //    let mut classes = HashMap::new();
    //
    //    walk(0, dom.document, initial.clone(), &mut classes);

    //    println!("{:?}", initial);
    //    println!("classes {:?}", classes.get("infos"));
    //
    //    if !dom.errors.is_empty() {
    //        println!("\nParse errors:");
    //        for err in dom.errors.into_iter() {
    //            println!("    {}", err);
    //        }
    //    }
}
